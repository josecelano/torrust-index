--- packages/mudlark/src/traits/accumulator.rs
+++ replace - with / in <impl Accumulator for f32>::sub
@@ -254,17 +254,17 @@
 
     #[inline]
     fn add(self, other: Self) -> Self {
         self + other
     }
 
     #[inline]
     fn sub(self, other: Self) -> Self {
-        self - other
+        self / /* ~ changed by cargo-mutants ~ */ other
     }
 }
 
 impl Accumulator for f64 {
     #[inline]
     fn zero() -> Self {
         0.0
     }
