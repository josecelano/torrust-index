--- packages/mudlark/src/view.rs
+++ replace Node<C, V>::width -> C with Default::default()
@@ -445,14 +445,14 @@
     /// # let mut g = GvGraph::<u64, u64, 8>::new(cfg);
     /// # g.observe(42, 10u64);
     /// let (_layer, node) = g.layers().next().unwrap();
     /// assert_eq!(node.width(), node.end - node.start);
     /// ```
     #[inline]
     #[must_use]
     pub fn width(&self) -> C {
-        C::width(self.start, self.end)
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[cfg(test)]
 mod tests {}
