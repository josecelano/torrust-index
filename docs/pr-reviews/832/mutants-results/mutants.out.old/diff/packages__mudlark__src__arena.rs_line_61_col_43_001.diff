--- packages/mudlark/src/arena.rs
+++ replace + with * in Arena<T>::alloc
@@ -53,17 +53,17 @@
             self.slots[i] = value;
             i
         } else {
             let i = self.slots.len();
             self.slots.push(value);
             // Grow bitset if needed.
             let word = i / 64;
             if word >= self.occupied.len() {
-                self.occupied.resize(word + 1, 0);
+                self.occupied.resize(word * /* ~ changed by cargo-mutants ~ */ 1, 0);
             }
             i
         };
         self.set_occupied(index, true);
         self.count += 1;
         index
     }
 
