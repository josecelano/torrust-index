--- packages/mudlark/src/traits/coordinate.rs
+++ replace >= with < in <impl Coordinate for f64>::is_final
@@ -315,17 +315,17 @@
 
     #[inline]
     fn width(start: Self, end: Self) -> Self {
         end - start
     }
 
     #[inline]
     fn is_final(_start: Self, _end: Self, depth: u32, n: u32) -> bool {
-        depth >= n
+        depth < /* ~ changed by cargo-mutants ~ */ n
     }
 
     #[inline]
     #[allow(clippy::cast_precision_loss)]
     fn from_u64(v: u64) -> Self {
         v as Self
     }
 
