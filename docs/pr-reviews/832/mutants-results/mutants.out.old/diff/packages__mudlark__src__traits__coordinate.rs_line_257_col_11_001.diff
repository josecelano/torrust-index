--- packages/mudlark/src/traits/coordinate.rs
+++ replace + with * in <impl Coordinate for f32>::midpoint
@@ -249,17 +249,17 @@
     #[inline]
     #[allow(clippy::cast_possible_wrap)]
     fn domain_max(n: u32) -> Self {
         2.0_f32.powi(n as i32)
     }
 
     #[inline]
     fn midpoint(a: Self, b: Self) -> Self {
-        a + (b - a) / 2.0
+        a * /* ~ changed by cargo-mutants ~ */ (b - a) / 2.0
     }
 
     #[inline]
     fn width(start: Self, end: Self) -> Self {
         end - start
     }
 
     #[inline]
