--- packages/mudlark/src/traits/accumulator.rs
+++ replace <impl Accumulator for f64>::zero -> Self with Default::default()
@@ -261,17 +261,17 @@
     fn sub(self, other: Self) -> Self {
         self - other
     }
 }
 
 impl Accumulator for f64 {
     #[inline]
     fn zero() -> Self {
-        0.0
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 
     #[inline]
     fn add(self, other: Self) -> Self {
         self + other
     }
 
     #[inline]
