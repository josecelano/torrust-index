--- packages/mudlark/src/testing/builder.rs
+++ replace GraphCreator<C, V>::config -> &Config<V> with Box::leak(Box::new(Config::from_iter([Default::default()])))
@@ -275,17 +275,17 @@
     #[must_use]
     pub fn plan(self) -> Plan<C, V> {
         self.plan
     }
 
     /// Extract the config.
     #[must_use]
     pub const fn config(&self) -> &Config<V> {
-        &self.config
+        Box::leak(Box::new(Config::from_iter([Default::default()]))) /* ~ changed by cargo-mutants ~ */
     }
 
     /// Execute the plan and return the graph.
     ///
     /// If `check_every` was set, `assert_invariants` is called at
     /// the specified interval.
     #[must_use]
     pub fn build<const N: u32>(self) -> GvGraph<C, V, N> {
