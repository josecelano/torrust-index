--- packages/mudlark/src/testing/config.rs
+++ replace cascade_config -> Config<u64> with Config::new(0)
@@ -90,22 +90,17 @@
         depth_evict: 8,
         ..BASE_U64
     }
 }
 
 /// Medium threshold for cascade testing: θ=3.
 #[must_use]
 pub const fn cascade_config() -> Config<u64> {
-    Config {
-        split_threshold: 3,
-        depth_create: 4,
-        depth_evict: 8,
-        ..BASE_U64
-    }
+    Config::new(0) /* ~ changed by cargo-mutants ~ */
 }
 
 /// Deep config for escalation testing: θ=2, `D_create`=5, `D_evict`=10.
 #[must_use]
 pub const fn deep_config() -> Config<u64> {
     Config {
         split_threshold: 2,
         depth_create: 5,
